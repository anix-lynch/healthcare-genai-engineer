# Vertex AI — text-embedding-005, Agent Builder, Grounding
# Activated when EMBEDDING_BACKEND=vertex
google-cloud-aiplatform>=1.60

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
numpy>=1.26
weave>=0.51

# Optional — hybrid_retriever falls back to BM25-only if missing.
# sentence-transformers>=2.5

# Testing
pytest>=7.4
httpx>=0.27
